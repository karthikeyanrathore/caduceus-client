import Navbar from './../components/Navbar';
import Container from './../components/Container';

const Appointment = () => {
	return (
		<div className="h-screen">
			<Navbar />
			<Container screen="appointment" />
		</div>
	);
};

export default Appointment;
